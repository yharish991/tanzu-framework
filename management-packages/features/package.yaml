apiVersion: data.packaging.carvel.dev/v1alpha1
kind: Package
metadata:
  name: features.tanzu.vmware.com.0.0.8
  namespace: features
spec:
  refName: features.tanzu.vmware.com
  version: 0.0.8
  releaseNotes: "relevant release notes..."
  licenses:
    - "UNKNOWN"
  template:
    spec:
      fetch:
        - imgpkgBundle:
            image: # Get this value by pushing to your OCI Registry
      template:
        - ytt:
            paths:
              - config/
        - kbld:
            paths:
              - "-"
              - .imgpkg/images.yml
      deploy:
        - kapp: {}
